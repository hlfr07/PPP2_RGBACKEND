import { ValideitorInterceptor } from './valideitor.interceptor';

describe('ValideitorInterceptor', () => {
  it('should be defined', () => {
    expect(new ValideitorInterceptor()).toBeDefined();
  });
});
